
function Order() {
  this.orders = [],
  this.orderId = 0,
}

Order.prototype.addOrder = function(order) {
  oredr.id = this.orderId()
  this.orders.push(order)
}







function Pizza() {
  this.toppings = topping,
  this.size = size,
  this.price = 0,
}
